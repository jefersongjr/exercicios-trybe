const sum = (num1, num2) => console.log(`${num2 + num1}`)

const sub = (num1, num2) => console.log(`${num2 - num1}`)

const div = (num1, num2) => console.log(`${num2 / num1}`)

const mult = (num1, num2) => console.log(`${num2 * num1}`)

module.exports = {
    sum, 
    sub,
    div,
    mult
};